## Corpus reproduction

The 77 PDF papers are not committed (arXiv-redistributable, ~80MB). To reproduce the corpus, the arXiv IDs are in `stage_1/data/manifest.json`. Download each by hitting `https://arxiv.org/pdf/<id>.pdf` and save to `paper_pdfs/<id>.pdf`. A small download script using the manifest IDs is a 15-line exercise.